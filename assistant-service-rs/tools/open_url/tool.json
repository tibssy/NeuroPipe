{
  "name": "open_url",
  "description": "Open a URL. Website links open in the default web browser. Video or audio links (YouTube, Vimeo, Dailymotion, mp4/webm/mp3 files) are played with mpv, which starts playback immediately and can then be controlled with media_control. Use this when the user names a link, video, or site to open.",
  "version": 1,
  "parameters": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "The URL to open"
      }
    },
    "required": ["url"]
  },
  "default_permission": "deny"
}
