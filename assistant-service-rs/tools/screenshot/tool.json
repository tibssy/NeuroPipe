{
  "name": "screenshot",
  "description": "Capture a screenshot and extract text via OCR. Use when the user asks about errors, code, terminal output, or anything visible on their screen.",
  "version": 1,
  "parameters": {
    "type": "object",
    "properties": {
      "region": {
        "type": "string",
        "enum": ["focused", "screen", "select"],
        "description": "Which area to capture: 'focused' (default) captures the active window, falling back to the active or default screen; 'screen' captures the active display; 'select' lets the user pick an area"
      }
    },
    "required": ["region"]
  },
  "default_permission": "ask"
}
