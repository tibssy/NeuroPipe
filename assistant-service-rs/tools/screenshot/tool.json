{
  "name": "screenshot",
  "description": "Capture a screenshot and extract text via OCR. Use when the user asks about errors, code, terminal output, or anything visible on their screen.",
  "version": 1,
  "parameters": {
    "type": "object",
    "properties": {
      "region": {
        "type": "string",
        "enum": ["focused", "screen", "select"],
        "description": "Which area to capture: 'focused' for the active window, 'screen' for the full display, 'select' for interactive area selection"
      }
    },
    "required": ["region"]
  },
  "default_permission": "ask"
}
