{
  "name": "web_search",
  "description": "Search the web for current information. Use when the user asks about news, facts, weather, or anything requiring up-to-date knowledge. Only accepted parameters are 'query' (the search string) and 'max_results' (number of results, default 5, max 10). No other parameters are supported.",
  "version": 1,
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "The search query"
      },
      "max_results": {
        "type": "integer",
        "description": "Number of results to return (default 5, max 10)"
      }
    },
    "required": ["query"]
  },
  "default_permission": "ask"
}
