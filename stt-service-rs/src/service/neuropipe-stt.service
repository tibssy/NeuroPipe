[Unit]
Description=NeuroPipe STT Service (Native Rust)
# Wait for sound system to be ready
After=sound.target pipewire.service pulseaudio.service

[Service]
Type=simple
# %h expands to home directory /home/username
WorkingDirectory=%h/.local/bin
ExecStart=%h/.local/bin/neuro-stt-service

# Restart automatically if it crashes
Restart=always
RestartSec=3

# Optimization: High priority for audio processing
Nice=-10

[Install]
WantedBy=default.target
